LISTENER=
  (DESCRIPTION_LIST =
    (DESCRIPTION=
      (ADDRESS_LIST=
        (ADDRESS=(PROTOCOL=tcp)(HOST={{ ip_address }})(PORT = {{ port }}))
        (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC1521))
      )
    )
  )
